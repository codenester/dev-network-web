import { Card, CardContent } from "@mui/material";
import { FC } from "react";

const Dashboard: FC = () => {
  return (
    <Card>
      <CardContent>
        Dashboard
      </CardContent>
    </Card>
  )
}